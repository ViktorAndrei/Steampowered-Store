/* Requires the Docker Pipeline plugin */
pipeline {
    agent any 
    stages {
        stage("build") {
            steps {
                echo 'något något'
                //sh 'python --version'
            }
        }
    }
}
